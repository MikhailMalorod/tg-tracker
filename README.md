# 🤖 Work Tracker Bot для Telegram

Полнофункциональный бот для отслеживания рабочего времени в Telegram с расширенными возможностями учета, анализа и автоматизации рабочего процесса.

## ✨ Функциональность

### 🔧 Основные возможности
- **Отслеживание времени**: автоматический учет рабочих сессий с категориями
- **Перерывы и паузы**: гибкое управление рабочим временем
- **Заметки с категориями**: быстрые записи с тегированием по типам
- **Детальная статистика**: анализ продуктивности за день/неделю/месяц
- **Экспорт данных**: выгрузка отчетов в CSV формате
- **Автоматические напоминания**: уведомления о длительной работе и перерывах
- **Интерактивный календарь**: визуальное представление рабочей активности
- **Конфигурируемые категории**: настройка категорий через JSON файл

### 📊 Расширенные функции
- **Автоматическая категоризация** рабочих сессий и заметок
- **Мониторинг продуктивности** с персональными целями
- **Фоновая синхронизация** с автоматическим обновлением данных
- **Горячая перезагрузка** категорий без перезапуска бота
- **Безопасная миграция** базы данных при обновлениях

## 🚀 Быстрый старт

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/MikhailMalorod/tg-tracker.git
   cd tg-tracker
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте переменные окружения:**
   Создайте файл `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен_бота
   DATABASE_PATH=work_tracker.db
   ```

4. **Запустите бота:**
   ```bash
   python bot.py
   ```

### Первоначальная настройка категорий

При первом запуске бот автоматически создаст файл `categories.json` с категориями по умолчанию. Вы можете изменить их в соответствии с вашими потребностями.

## 📋 Команды бота

### Основные команды
- `/start` - Приветствие и главное меню
- `/start_work` - Начать рабочую сессию с выбором категории
- `/end_work` - Завершить рабочую сессию
- `/break` - Сделать перерыв (с указанием причины)
- `/resume` - Продолжить работу после перерыва

### Работа с заметками
- `/note` - Создать заметку с выбором категории
- `/my_notes` - Просмотреть последние заметки

### Анализ и отчеты
- `/stats` - Детальная статистика (день/неделя/месяц)
- `/export` - Экспорт всех данных в CSV файл
- `/calendar` - Интерактивный календарь работы

### Настройки и управление
- `/reminders` - Управление автоматическими напоминаниями
- `/categories` - Управление категориями (просмотр/перезагрузка)

## 🎯 Категории

### Категории работы (по умолчанию)
- **Разработка** - Программирование, кодинг
- **Совещания** - Встречи, обсуждения
- **Документация** - Написание документации
- **Обучение** - Изучение нового материала
- **Другое** - Прочие активности

### Категории заметок (по умолчанию)
- **Общее** - Общие заметки
- **Идея** - Идеи и предложения
- **Задача** - Рабочие задачи
- **Проблема** - Проблемы и сложности
- **Встреча** - Заметки о встречах
- **Личное** - Личные заметки

## 🔔 Автоматические напоминания

Бот автоматически отправляет напоминания:

- **Работа**: если сессия длится слишком долго (по умолчанию 60 мин)
- **Перерыв**: предложение сделать перерыв (по умолчанию 15 мин)
- **Длинный перерыв**: для очень длительных сессий (по умолчанию 120 мин)
- **Ежедневная цель**: поздравление с достижением цели (по умолчанию 480 мин)

Настройки можно изменить командой `/reminders`.

## 📊 Экспорт данных

Команда `/export` создает CSV файл со всеми данными:
- Информация о пользователе
- Все рабочие сессии с категориями
- Заметки с категориями и сессиями
- Перерывы с причинами

## 🔧 Конфигурация категорий

### Изменение категорий
Отредактируйте файл `categories.json`:
```json
{
  "work_categories": [
    "Разработка",
    "Дизайн",
    "Менеджмент",
    "Поддержка"
  ],
  "note_categories": [
    "Идея",
    "Задача",
    "Баг",
    "Вопрос"
  ]
}
```

### Автоматическая перезагрузка
- Бот мониторит изменения файла каждые 5 минут
- Изменения применяются автоматически
- Принудительная перезагрузка: `/categories` → "Перезагрузить категории"

## 🗄️ База данных

База данных SQLite автоматически:
- Создает таблицы при первом запуске
- Добавляет новые колонки при обновлениях
- Миграция данных при изменении схемы
- Безопасное обновление существующих записей

## 🛠️ Технологии

- **Python 3.8+** - асинхронное программирование
- **python-telegram-bot 22.5** - Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **python-dotenv** - управление конфигурацией
- **SQLite** - локальное хранение данных

## 📈 Архитектура

- **Agent-First**: асинхронная обработка запросов
- **Service-Oriented**: модульная структура кода
- **Stateless**: отсутствие состояния между запросами
- **Tool-Centric**: инструменты для работы с данными
- **Secure**: валидация всех входных данных

## 🔒 Безопасность

- Валидация всех пользовательских данных
- Безопасная обработка SQL запросов
- Защита от инъекций и XSS атак
- Логирование действий пользователей

## 🚀 Развертывание

### Локальное развертывание
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python bot.py
```

### Развертывание на сервере
1. Настройте переменные окружения на сервере
2. Установите зависимости в виртуальное окружение
3. Запустите бота с помощью systemd/supervisor
4. Настройте мониторинг логов

### Docker развертывание (рекомендуется)
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "bot.py"]
```

## 📝 Лицензия

Этот проект является open-source и доступен для использования и модификации.

## 🤝 Поддержка

При возникновении проблем или вопросов:
1. Проверьте логи бота
2. Создайте issue в репозитории
3. Обратитесь к документации

---

**Версия**: 2.0 | **Последнее обновление**: Январь 2025
